<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #8791a3">
    <a class="navbar-brand" href="{{ path('homepage') }}">Marseille
        Construction</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            {% if is_granted("ROLE_RH") or
                is_granted("ROLE_CONDUCT_TRVX") %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('employee_indexWorkers')
                    }}">R H</a>
                </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('constructionSites_index')
                }}">Chantiers</a>
            </li>
            {% if is_granted("ROLE_RESP_MATERIEL") or
                is_granted("ROLE_CONDUCT_TRVX") %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('materials_index')
                }}">Matériels</a>
            </li>
            {% endif %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ app.user.firstName | capitalize ~ ' ' ~ app.user
                    .lastName | capitalize }}
                </a>
                <div class="dropdown-menu dropdown-menu-right"
                     aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('userEdit',
                        {id: app.user.id})
                    }}">Mes paramètres</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Se
                        déconnecter</a>
                </div>
            </li>
        </ul>
    </div>
</nav>